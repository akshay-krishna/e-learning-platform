import TextField from "@material-ui/core/TextField";
const { Fragment } = require("react");

const DragDrop = () => {
  return (
    <Fragment>
      <TextField type="file" />
    </Fragment>
  );
};

export default DragDrop;
